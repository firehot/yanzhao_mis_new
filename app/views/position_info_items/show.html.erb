<div class="block">
  <div class="secondary-navigation">
    <%= render "caption" %>
  </div>
  <div class="content">
    <div class="inner">
      <h2 class='text-center'><%=@position_info_item.title%></h2>
      <p class='text-center'><%=@position_info_item.created_at.strftime('%Y-%m-%d')%> </p>
      <hr />
      <p>
        <%= @position_info_item.content %>
      </p>
      <hr />
    </div>
  </div>
</div>
<% content_for :sidebar, render(:partial => 'sidebar') %>
